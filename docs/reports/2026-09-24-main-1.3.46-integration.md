# Main v1.3.46 rebase and issue coverage

2026-09-24 (UTC+08:00). Source integration only; not production-readiness or cache-efficiency certification.

## Baseline and history

Main target `2e9d7d0` (v1.3.46) contains 13 commits after `607fdd6`. Prior local test `0de66ec` is preserved at `backup/test-before-main-rebase-20260924-165518`; remote test remains `430cb43`. No push, deployment, service/native execution, inference or alteration of the historical patch/CPA checkout occurred.

Six commits replayed without conflicts as `93d4e92`, `1a695f1`, `a3ff2d2`, `d8fb131`, `26861ae`, `6786584`. Range-diff shows the first five unchanged and the sixth differing only because main already removed a trailing blank line. Main's panel additions coexist with the fork's auto-TTL validation and protocol documentation. No new production correction was needed.

## What main fixes — and does not

- Adds ordered DNS upstream fallback and operator-selectable primary, including propagation into egress configuration. This can address resolver failures; it does not prove improved prompt-cache utilization.
- Preserves manually selected concurrency/RPM during routing/tier synchronization by reading raw VM override flags.
- Reports actual slot process/telemetry state rather than treating configured enablement as evidence of a running process.
- Persists passive probe-check display metadata separately. Reading cached headers does not heal failed authentication, clear authoritative backoff or invent fresh quota samples.
- Does **not** change the earlier OAuth fatal-refresh-code loss or crag wrap-materializer mismatch. Those remain operational risks affecting credential retirement and selected kernel/config consistency respectively.
- Does **not** replace the fork's Chat finish/error handling, EOF/UTF-8/terminal metadata checks, measured usage, normalized cache/auxiliary TTL policy, official-mask reset or committed/cancelled retry guards. All remain retained.

The Node changes and available Go DNS source were inspected; no shipped egress/native program was executed. Final native cache markers, installed versions, hidden calls and the user's warm-request approximately 11% quota behavior remain unverified.

## Test-only compatibility correction

The new upstream DNS-config test hardcoded `/bin/true`, which is absent in native Windows. It failed in both frozen main and the initial merged suite. The test now supplies an existing executable path and mocks only child-process spawning, with builtin ESM bindings refreshed/restored. Actual config serialization remains under test; both DNS-field assertions are retained and exact spawn arguments are checked. No process is started and no production API is changed.

## Validation

- Targeted shared/new-upstream run: 201 tests, 198 passed, zero failed, three existing Windows shell-observation skips.
- Corrected egress file: 12/12 passed.
- Frontend snapshot: 130/130 tests, formatting and production build passed; source matches the rebased frontend.
- Full Node baselines: main 25 failures across 8/159 files; prior test 25 across 8/168. Initial merged run had their union (26 across 9/171), including the inherited `/bin/true` fixture. After test correction, final run has the same prior-test 25 named failures across 8/171, with no new failure against the baselines. This is not a full-green result.
- Independent source review found no newly introduced rebase blocker and verified the new main behavior alongside the retained fork contracts. The follow-up covers the test-only fixture change separately.
- No Go/platform validation retry was attempted; the previously documented Unix-flock/Windows limitation remains. No Go or shipped-binary changes beyond upstream were introduced.

Evidence bundle: `C:/Users/zemingxi/AppData/Local/Temp/vm2api-rebase-20260924-1655/` (frozen inputs, range-diff, baseline/final logs, fixture failure and correction, frontend logs and independent reviews). Known unresolved upstream issues are detailed in the preceding v1.3.44 report and current context. Keep default wrap unchanged; source/config tests are not authorization or proof of safe crag activation.
