# Current context: R3 acceptance, CC promotion, account-tab failure

2026-09-26 (UTC+08). User supplied CC/Crag r3 captures and conditionally authorized promotion only if comparison passes. Also reports a500 on opening a VM's Account tab. Main agent only; no running-slot changes, real-provider requests, push or deployment.

## Current rebase: v1.3.59 (2026-09-26)
- Rebased test8d4d563 onto origin/main d9f31c8. Backup: backup/test-before-main159-20260926-2120. Five local commits replayed as4ad8172 /2a9b9e9 /fe04de1 /bb42b48 /8a72000.
- One import conflict in usage-logs-repo: retained both the new window helpers and existing cacheCreationUsage. Upstream adds aligned billing windows, Extra calibration and OAuth identity/type handling. Fixed/candidate/native assets, caller/Chat safety, raw access controls, account-tab repair, closed round and Docker inputs remain unchanged.
- Targeted checks224/224 plus10 fake-session/interface controls passed; frontend159 tests, format and build passed. Linux executable helper seam cases, the full suite, Docker/native execution and real upstream calls were not run. No subagents, push or deployment.
- Evidence: C:/Users/zemingxi/AppData/Local/Temp/vm2api-rebase159-20260926-2120/. Final upstream check stilld9f31c8.

## Baseline / latest upstream
- User checkout testb00862f differs from prior31bbb14 only in two tests. Rebased its three commits onto origin/main499ded1(v1.3.58), producing af5d591/48619c5/23e01b6. Backup: backup/test-before-cc-promotion-20260926-195703.
- The upstream kernel-pipeline change was merged then reverted; net bin/share diff fromcb89545 to499ded1 is empty. Do not rebuild against an already reverted binary. Latest substantive delta removes the remote default backend and adjusts notifications/UI. The existing tested v155 native hashes still apply.

## Acquired evidence
- logs/vm2api-raw-debug-cand-cc-r3.jsonl and -cand-crag-r3.jsonl contain full untruncated r3 traces, CLI hash prefix71010c5b and confirmed uploaded bytes. Both phases in both traces preserve the16069-codepoint synthetic reply and report completion; each real-CLI phase sends one mock API request.
- CC final request preserves caller system99926 codepoints and all245 messages exactly, Opus4.6/128000/adaptive+summarized/effort=max. Kernel reply bytes and assembled Messages equal the accepted CLI reference; Chat differs only in created. Framework environment and both1h cache anchors match, and native CCH recomputes correctly. Differences retained: context_management clear_thinking/keep=all, fresh device/session IDs, and CC billing cc_version=2.1.280.104, cc_is_subagent=true, cc_turn_origin=sdk. The attribution generator is byte-identical to original CC. This is not full cli-node equivalence or real quota validation.
- Crag fails request-semantic acceptance despite response completeness: final API has one user message, caller system embedded as user text, max_tokens64000, adaptive without display, and no output_config.effort. History preservation requires exact inspection; do not promote Crag as equivalent on the strength of a success status.
- Account tab reproduction: the full React subtree throws Objects are not valid as a React child for a persisted probe.error object, which the root error component displays as500. The backend can persist that object unchanged. probeOutcome now uses the existing safe message normalizer and returns a string; absent/string/object cases render the import controls. Unrecognized object metadata is not dumped. This confirms one reachable frontend cause; a true HTTP500 still requires the user's endpoint/response (clarification requested). Credential plaintext remains admin-only and lazy-loaded.
- Evidence directory: C:/Users/zemingxi/AppData/Local/Temp/vm2api-r3-acceptance-20260926/. Source logs and previous approved/candidate bundles remain immutable.

## Local implementation and verification
- Added selectable cc-fixed, backed by share/cc-fixed/v155-r3. Only183 guard bytes differ from the accepted R3 uncompressed CLI; CC initialization and every other repair remain intact. Reuses the common approved-bundle loader/install/recovery/sync paths, keeps native family cc, and installs .kin/cc-node-fixed with the tested kernel. Existing default/config and approved wrap-fixed/candidates unchanged.
- Crag not promoted: only original message index244 survives as a user block; caller system is another user block. No new Crag binary or semantic repair is claimed.
- Current candidate round closed with an empty list. The UI supports empty rounds, does not auto-disable existing offline mode, and refuses re-enabling an empty round. Backend remains fail-closed; normal inference resumes only after explicit user disable. Historical low-level tests use an in-memory fixture round, never rewrite the shipped catalog.
- Guard controls6/6 pass (candidate control5 failures/1 unaffected positive); data-plane first tests6 red/1 retained refusal control then green. Account subtree3 cases and probe formatting controls pass after the structured-object red case. Closed-round controls2 red -> green.
- Final targeted Node312/0fail/0skip, Python17, frontend159 plus format/build pass. A fresh isolated web-stage layout containing only web and the shared JSON also builds. No full suite, local Linux ELF/Docker run, provider call, push or deployment. Build/evidence logs remain in the directory above.

## Bounded plan and invariants
- Finish full request/response comparison using real Node decoding and the existing accepted CLI trace. Only an accepted combination becomes selectable for normal inference; retain all differences and validation limits in its manifest.
- Reproduce Account tab failure with the actual React subtree and realistic backend data shapes; add discriminating red/green tests and fix the responsible boundary, not a blanket catch or disabled UI. No live credentials are read by tests.
- For an accepted CC bundle, add `cc-fixed` alongside existing production choices with explicit user selection, no default change. Reuse the fixed-bundle installation/config/sync lifecycle rather than add another lifecycle. Pin the existing tested wrap kernel and install a distinct .kin/cc-node-fixed; missing/hash/approval failures must not fall back.
- A CC promotion must remove only the r3 offline guard while retaining CC initialization, system/workload/debug/error-detail fixes. Restoring the original whole entry would reintroduce Config accessed before allowed. Verify unchanged bytes, full JS/ELF/UPX roundtrip and extracted entry/control behavior; do not claim local Linux execution.
- Crag remains unpromoted unless request preservation can be demonstrated. Close tested/known-failed items out of the public current-round picker; an empty round must keep offline mode fail-closed until the user explicitly disables it, never silently restore real inference. Historical assets/logs remain available for analysis, not permanently stacked in the picker.
- Run focused Node/Python/UI checks and an isolated Docker-web-input build. No broad audit/subagent loop or full-suite requirement. Document exactly what was accepted, withheld and not run, then commit intended changes only.
